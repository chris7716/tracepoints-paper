# cigzip / FastGA run

- Timestamp: `2025-11-09T04:57:09-06:00`

## Inputs

| Parameter | Value |
|---|---|
| record_count (-r) | 10000 |
| length (-L) | 10000 |
| error (-E) | 0.05 |
| N (-N) | 11 |
| type (-T) | standard |
| complexity metric (cm) | edit-distance |
| max complexity (mc) | 32 |
| threads | 8 |

## Tool Binaries

```text
WFA2_BIN   : ../WFA2-lib/bin/generate_dataset
FASTGA_BIN : ../FASTGA/FastGA
CIGZIP_BIN : ../cigzip/target/debug/cigzip

# WFA2 help/version (if any):
USE: ./generate-datasets [OPTIONS]...
      Options::
        --output|o         PATH        Output path of the generated sequences
        --num-patterns|n   INT         Total number of sequence-pairs generated
        --length|l         INT         Length of the pattern (pattern.length) 
(no output)

# FastGA banner (no -h):

Usage: FastGA [-vkMS] [-L:<log:path>] [-T<int(8)>] [-P<dir($TMPDIR)>] [<format(-paf)>]
              [-f<int(10)>] [-c<int(85)> [-s<int(1000)>] [-l<int(100)>] [-i<float(.7)]
              <source1:path>[<precursor>] [<source2:path>[<precursor>]]

(no output)

# cigzip help (if any):
Usage: cigzip <COMMAND>

Commands:
  encode  Encode alignments into tracepoints
  decode  Decode tracepoints back to CIGAR
```

## Pipeline


### 1) WFA2 generate_dataset

**Command**:
```bash
../WFA2-lib/bin/generate_dataset -n 10000 -l 10000 -e 0.05 -o wf-dataset/sample.dataset.11.seq
```
**Output**:
```text
```

### 2) Convert to FASTA (awk)

**Command**:
```bash
awk '
BEGIN { counter=1 }
/^>/ { 
    print ">pattern_" counter
    print substr($0, 2)
}
/^</ { 
    print ">text_" counter
    print substr($0, 2)
    counter++
}' wf-dataset/sample.dataset.11.seq > wf-dataset/dataset.11.fa
```
**Output**:
```text
FASTA written: wf-dataset/dataset.11.fa
```

### 3) FastGA self-alignment → fastga-datasets/dataset.11.paf

**Command**:
```bash
../FASTGA/FastGA -P/home/hasitha/data/projects/fastga-tmp -pafx wf-dataset/dataset.11.fa wf-dataset/dataset.11.fa > fastga-datasets/dataset.11.paf
```
**Output**:
```text
```

### 4) cigzip encode (type=standard) → tmp/tmp.standard.11.paf

**Command**:
```bash
../cigzip/target/debug/cigzip encode -p fastga-datasets/dataset.11.paf --type standard --complexity-metric edit-distance --max-complexity 99999999 > tmp/tmp.standard.11.paf
```
**Output**:
```text
```

### 5) cigzip decode (type=standard) → cigzip-datasets/dataset.standard.11.paf

**Command**:
```bash
../cigzip/target/debug/cigzip decode -p tmp/tmp.standard.11.paf --type standard --complexity-metric edit-distance --sequence-files wf-dataset/dataset.11.fa > cigzip-datasets/dataset.standard.11.paf
```
**Output**:
```text
```

### 6) cigzip encode (max-complexity=32, threads=8) → cigzip-datasets/dataset.standard.11.tp.mc32.paf

**Command**:
```bash
../cigzip/target/debug/cigzip encode -p cigzip-datasets/dataset.standard.11.paf --type standard --complexity-metric edit-distance --max-complexity 32 -t 8 > cigzip-datasets/dataset.standard.11.tp.mc32.paf
```
**Output**:
```text
```

### 7) cigzip decode + diff verification

**Command**:
```bash
/usr/bin/time -v ../cigzip/target/debug/cigzip decode -p cigzip-datasets/dataset.standard.11.tp.mc32.paf --type standard --complexity-metric edit-distance --sequence-files wf-dataset/dataset.11.fa --max-complexity 32 -t 8 > cigzip-datasets/dataset.standard.11.tp.mc32.decompressed.paf && diff <(sort cigzip-datasets/dataset.standard.11.paf) <(sort cigzip-datasets/dataset.standard.11.tp.mc32.decompressed.paf) | wc -l
```
**Output**:
```text
Command being timed: "../cigzip/target/debug/cigzip decode -p cigzip-datasets/dataset.standard.11.tp.mc32.paf --type standard --complexity-metric edit-distance --sequence-files wf-dataset/dataset.11.fa --max-complexity 32 -t 8"
	User time (seconds): 545.15
	System time (seconds): 13.24
	Percent of CPU this job got: 795%
	Elapsed (wall clock) time (h:mm:ss or m:ss): 1:10.19
	Average shared text size (kbytes): 0
	Average unshared data size (kbytes): 0
	Average stack size (kbytes): 0
	Average total size (kbytes): 0
	Maximum resident set size (kbytes): 35212
	Average resident set size (kbytes): 0
	Major (requiring I/O) page faults: 0
	Minor (reclaiming a frame) page faults: 65400
	Voluntary context switches: 1947
	Involuntary context switches: 903
	Swaps: 0
	File system inputs: 0
	File system outputs: 95568
	Socket messages sent: 0
	Socket messages received: 0
	Signals delivered: 0
	Page size (bytes): 4096
	Exit status: 0
0
```

## Artifacts


| File | Size (bytes) |
|---|---|
| wf-dataset/sample.dataset.11.seq | 200040028 |
| wf-dataset/dataset.11.fa | 200267816 |
| fastga-datasets/dataset.11.paf | 47123527 |
| tmp/tmp.standard.11.paf | 5714374 |
| cigzip-datasets/dataset.standard.11.paf | 48845434 |
| cigzip-datasets/dataset.standard.11.tp.mc32.paf | 7956841 |
| cigzip-datasets/dataset.standard.11.tp.mc32.decompressed.paf | 48845434 |
