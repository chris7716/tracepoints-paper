# cigzip / FastGA run

- Timestamp: `2025-11-24T20:58:27-06:00`

## Inputs

| Parameter | Value |
|---|---|
| record_count (-r) | 10000 |
| length (-L) | 1000 |
| error (-E) | 0.05 |
| N (-N) | 43 |
| type (-T) | fastga |
| complexity metric (cm) | edit-distance |
| max complexity (mc) | 100 |
| threads | 8 |

## Tool Binaries

```text
WFA2_BIN   : ../WFA2-lib/bin/generate_dataset
FASTGA_BIN : ../FASTGA/FastGA
CIGZIP_BIN : ../cigzip/target/debug/cigzip

# WFA2 help/version (if any):
USE: ./generate-datasets [OPTIONS]...
      Options::
        --output|o         PATH        Output path of the generated sequences
        --num-patterns|n   INT         Total number of sequence-pairs generated
        --length|l         INT         Length of the pattern (pattern.length) 
(no output)

# FastGA banner (no -h):

Usage: FastGA [-vkMS] [-L:<log:path>] [-T<int(8)>] [-P<dir($TMPDIR)>] [<format(-paf)>]
              [-f<int(10)>] [-c<int(85)> [-s<int(1000)>] [-l<int(100)>] [-i<float(.7)]
              <source1:path>[<precursor>] [<source2:path>[<precursor>]]

(no output)

# cigzip help (if any):
Usage: cigzip <COMMAND>

Commands:
  encode  Encode alignments into tracepoints
  decode  Decode tracepoints back to CIGAR
```

## Pipeline


### 1) WFA2 generate_dataset

**Command**:
```bash
../WFA2-lib/bin/generate_dataset -n 10000 -l 1000 -e 0.05 -o data/simulated/wf-dataset/sample.dataset.43.seq
```
**Output**:
```text
```

### 2) Convert to FASTA (awk)

**Command**:
```bash
awk '
BEGIN { counter=1 }
/^>/ { 
    print ">pattern_" counter
    print substr($0, 2)
}
/^</ { 
    print ">text_" counter
    print substr($0, 2)
    counter++
}' data/simulated/wf-dataset/sample.dataset.43.seq > data/simulated/wf-dataset/dataset.43.fa
```
**Output**:
```text
FASTA written: data/simulated/wf-dataset/dataset.43.fa
```

### 3) FastGA self-alignment → data/simulated/fastga-datasets/dataset.43.paf

**Command**:
```bash
../FASTGA/FastGA -P/home/hasitha/data/projects/fastga-tmp -pafx data/simulated/wf-dataset/dataset.43.fa data/simulated/wf-dataset/dataset.43.fa > data/simulated/fastga-datasets/dataset.43.paf
```
**Output**:
```text
```

### 4) cigzip encode (type=fastga) → data/simulated/tmp/tmp.fastga.43.paf

**Command**:
```bash
../cigzip/target/debug/cigzip encode -p data/simulated/fastga-datasets/dataset.43.paf --type standard --complexity-metric edit-distance --max-complexity 99999999 > data/simulated/tmp/tmp.fastga.43.paf
```
**Output**:
```text
```

### 5) cigzip decode (type=fastga) → data/simulated/cigzip-datasets/dataset.fastga.43.paf

**Command**:
```bash
../cigzip/target/debug/cigzip decode -p data/simulated/tmp/tmp.fastga.43.paf --type standard --complexity-metric edit-distance --sequence-files data/simulated/wf-dataset/dataset.43.fa > data/simulated/cigzip-datasets/dataset.fastga.43.paf
```
**Output**:
```text
```

### 6) cigzip encode (max-complexity=100, threads=8) --minimal → cigzip-datasets/dataset.fastga.43.tp.mc100.paf

**Command**:
```bash
/usr/bin/time ../cigzip/target/debug/cigzip encode -p data/simulated/cigzip-datasets/dataset.fastga.43.paf --type fastga --complexity-metric edit-distance --max-complexity 100 -t 8 --minimal > data/simulated/cigzip-datasets/dataset.fastga.43.tp.mc100.paf
```
**Output**:
```text
0.84user 0.27system 0:00.21elapsed 519%CPU (0avgtext+0avgdata 6652maxresident)k
0inputs+10608outputs (0major+515minor)pagefaults 0swaps
```

### 7) cigzip decode + diff verification

**Command**:
```bash
/usr/bin/time -v ../cigzip/target/debug/cigzip decode -p data/simulated/cigzip-datasets/dataset.fastga.43.tp.mc100.paf --type fastga --complexity-metric edit-distance --sequence-files data/simulated/wf-dataset/dataset.43.fa --max-complexity 100 -t 8 > data/simulated/cigzip-datasets/dataset.fastga.43.tp.mc100.decompressed.paf && diff <(sort data/simulated/cigzip-datasets/dataset.fastga.43.paf) <(sort data/simulated/cigzip-datasets/dataset.fastga.43.tp.mc100.decompressed.paf) | wc -l
```
**Output**:
```text
0
```

## Artifacts


| File | Size (bytes) |
|---|---|
| data/simulated/wf-dataset/sample.dataset.43.seq | 20040132 |
| data/simulated/wf-dataset/dataset.43.fa | 20267920 |
| data/simulated/fastga-datasets/dataset.43.paf | 7880432 |
| data/simulated/tmp/tmp.fastga.43.paf | 5377944 |
| data/simulated/cigzip-datasets/dataset.fastga.43.paf | 9508819 |
| cigzip-datasets/dataset.fastga.43.tp.mc100.paf | 5428535 |
| cigzip-datasets/dataset.fastga.43.tp.mc100.decompressed.paf | 9508819 |
